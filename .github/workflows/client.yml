name: Client Test

on:
    push:
        branches: [master]
    pull_request:
        branches: [master]

jobs:
    build:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [18.x]

        steps:
            - uses: actions/checkout@v3
            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v3
              with:
                  node-version: ${{ matrix.node-version }}
                  cache: "npm"
            - run: npm ci
            - run: npm run build-ts
            - run: npm run client-type-check
            - run: npx webpack --config client/webpack.config.cjs --mode=development # Build in development mode for the test selectors.
            - run: npm run fronttest-headless-bail
            - uses: actions/upload-artifact@v3
              with:
                name: screenshots
                path: *.png
